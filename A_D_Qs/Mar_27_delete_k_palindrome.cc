// waterrfetawx
// scanning over the first and the last element and if they are not the same,
// then decrement the number of k 
// waterrfetawx

bool palindrome_check (const string &s, int k)
{
	string copy(s);
	int i = 0, j = s.size()-1;
	while (i <= j)
	{
		if (s[i] != s[j])

	}
}

